# Changelog

このプロジェクトの主な変更を記録します。

## [Unreleased]

## [v2.5.1] - 2026-09-26

プレリリース。撫でたときの返事を、通常の顔画面でも日本語字幕で表示します。

### Fixed

- 字幕をプリンター情報の表示設定から分離。LOCAL LLM・LEVEL HOLD・MQTT/HUD OFFでも表示
- 撫でたときのHAPPY!/LOVE!を日本語にし、発話中は返事の字幕を保持。発話後も2.5秒表示
- 字幕表示中の顔の拡大・回転を抑え、画面下の文字の揺れや欠けを防止
- 起動あいさつ・通常の読み上げ・プリンター実況も日本語対応の共通字幕へ統一
  （本文の分からないGatewayの録音音声だけの再生は対象外）
- 既存の日本語フォント・字幕バッファを再利用。音声パックの更新は不要

## [v2.5.0] - 2026-09-26

プレリリース。頭を撫でると、笑顔・首振りとうなずき・セリフで喜ぶようになりました。

### Added

- 頭の前後を撫でると約6秒間喜び、続けて撫でるほど反応が3段階で大きくなる
- 「えへへ、なでなでうれしい！」など8種類の短い返事。内蔵ボイスを優先し、
  発話中も撫でる入力と喜びの動きを継続。音声パックの更新は不要
- プリンター詳細画面から一時的に笑顔へ切り替え、反応が終わると詳細画面へ戻る
- セリフの待ちは1件にまとめ、5.5秒以上の間隔を置く。古くなった返事は再生しない
- 頭の3領域の移動も判定し、BSPのスワイプ判定だけでは拾えないゆっくりした撫で方にも対応

### Changed

- 頭の前後スワイプを撫でる操作に統一し、配色切り替えとの重複を解消
- 撫でた後のクリック・長押しの誤動作を抑制。設定画面・校正・LEVEL HOLDの制御を優先
- 首の現在の目標を保ったまま演出を始め、校正範囲と1更新ごとの移動量を制限。
  v2.4.1のサーボ読取り失敗対策と、動作中に位置を再同期しない処理を維持
- 完了・エラーなど優先度の高いプリンター通知は優先。実況OFFでは声を出さず、表情と動きで反応
- 吹き出しの文字列更新も描画フレームのミューテックスで保護し、連続反応時の読み書き競合を防止

## [v2.4.1] - 2026-09-26

プレリリース。プリンター接続後に首が急に左を向き、ゆっくり中央へ戻る不具合を修正。

### Fixed

- サーボの位置読取り失敗（-1）を左端の角度（-1280）として使うBSPの処理を修正。
  不正な読取り値では現在の動作の推定位置を保持する。
  警告ログを有効にしたデバッグビルドでは、最大5秒に1回記録する
- LOCAL LLM / MQTTの切り替えで動作中の首制御を再初期化せず、位置と動きの途中状態を引き継ぐ
- 連続動作中はUARTの実測位置へ80msごとに同期せず、開始時だけ同期して滑らかに動かす
- 固定依存StackChan-BSP 1.1.0の修正をビルド時に自動適用。依存を再取得しても修正が残る。
  `python tools/motion_test/run.py --compare-v240` で旧版のエラー値→左端を再現し、修正後を検証

## [v2.4.0] - 2026-09-26

プレリリース。UIの描画負荷とメモリを減らし、内蔵音声の長音・抑揚を改善。

### Changed

- 本体UIの共有キャンバスをRGB332の8bitに変更（320×240で150KiB→75KiB）
- 8bit化後も背景・カード・枠線を区別できる配色に統一
- プリンター詳細画面は表示値が変わった場合だけ再描画し、変化した16行単位の領域だけLCDへ転送。
  更新は最大5fps。字幕の折り返し結果をキャッシュし、顔の描画は最大20fpsに制限
- 音声再生バッファ9KiBをPSRAMへ移し、Wi-Fi/TLS用の内部RAMを確保。
  静的RAMは74,400→66,048B。音声処理追加によりFlash使用量は約5.6KB増加
- 自由文の母音にPSOLAを適用し、音程の連続変化・音長・句末の抑揚を調整。
  長音では元のモーラの母音部分を再利用。日本語・英語の読み辞書は従来どおりで、
  全単語の正読・自然さを保証するものではない。v2.3.xのボイスパックと互換

### Fixed

- 顔描画の「待機中」をフレーム終了と誤認する競合を修正。ライブラリはLCD転送中にも待機するため、
  フレーム全体をミューテックスで保護し、転送完了後にメニュー等へ表示を渡す。
  初期化時の2KB描画タスクも実行前に8KBタスクへ置き換える

### Added

- 作業履歴・現在の構成・実機診断結果・再開手順を `agent.md` に記録
- `tools/monitor_serial.py`: DTR/RTS を下げてシリアルを記録し、USB 切断後は再接続。
  時刻付きログとヒープ・スタック残量等の JSON 集計を保存。切断中の通知連発を抑制
- 音声波形テストと本体共通処理でのPC試聴用WAV。`--legacy`で旧方式と比較可能
- `/api/status`に`ui_frames`・`ui_pixels`・`uptime_ms`を追加（描画量と再起動の診断用）

## [v2.3.2] - 2026-09-25

プレリリース。ときどき再起動する不具合を直しました。

### Fixed

- 顔にプリンターの HUD や字幕を描いているときに、顔の描画タスク（m5stack-avatar の drawLoop、スタック 2KB 固定）の
  スタックがあふれて再起動していた（コアダンプで確認）。描画タスクをスタック 8KB の同等のタスクに差し替えた。
  実測の最大使用量は約 2.7KB

### Changed

- 15 秒ごとのシリアルログ `[heap]` に、顔の描画・メインループ・MQTT の各タスクのスタックの残りを追加

## [v2.3.1] - 2026-09-25

プレリリース。本体の UI の不具合を直しました。

### Fixed

- 本体の設定画面（キーボード）でクラッシュする（PANIC）不具合。メニューや設定画面の裏で顔の描画が
  再開し、LCD の SPI バスを取り合っていた。顔の描画は「フレームを描き終えて待機に入ったところ」でだけ
  止め、止めている間の表情の変更は再開時にまとめて反映する（m5stack-avatar の `setExpression()` が
  描画を必ず再開するため）
- メニューを開くと別の画面が一瞬見えて顔に戻ることがある不具合（同じ原因）

### Changed

- 顔の描画タスクの優先度をメインループより下げ、メニュー・設定画面・タッチを優先する
- メニューを整理: 設定（Wi-Fi・プリンター・接続情報）を「設定」1つにまとめた。プリンター未設定のときに
  「MQTT」「プリンター」を押すと、同じ設定画面を開かずに登録場所を案内する
- MQTT モードに入ったとき（印刷開始の自動切り替え・メニュー・Web）は顔ではなくプリンター詳細画面を出す。
  ほかのモードに戻ると顔に戻る

## [v2.3.0] - 2026-09-25

プレリリース。内蔵ボイスが日本語・英語のどんな文章でも読めるようになりました。

### Added

- 読み辞書をファームウェアに埋め込み（`dict/ja.dic` 2.8MB: mecab-naist-jdic 由来、`dict/en.dic` 0.66MB:
  CMU 発音辞書の頻出語と綴りから発音を推定する決定木）。`tools/make_dict.py` で生成
- `src/talk/`: 日本語の区切りと読み（単語コスト＋256 クラスタに縮約した連接コストの Viterbi）、
  Open JTalk の規則によるアクセント句・アクセント核、母音の無声化、数と助数詞の音の変化、
  英単語のカタカナ読み（辞書・決定木・ローマ字・アルファベット）、記号の読み
- ボイスパックにモーラの音（141 音 × 低・高、無声化）を追加。「ア＋モーラ＋同じ母音」を平らな音程で
  合成して真ん中を切り出し、音程による大きさの差をそろえる
- クリップだけで読めない文はモーラを 5ms 重ねてつなげて読む。ジョブ名（「」内）も読む（「作品」と言い換えない）
- `tools/talk_test/`: 同じ処理を PC で動かすテスト（VOICEVOX の読みとの比較、`--wav` で音声のプレビュー）
- 設定・状態ページに内蔵ボイスがどんな文章も読めるかを表示

### Changed

- パーティション構成を変更（アプリ 5.75MB + LittleFS 10.1MiB）。**ボイスパックの作り直しと `uploadfs` が必要**
- Gateway 用の指示（`__CURRENT__` など）を内蔵ボイスで読むときは、あいさつに置き換える（`__SAY__` は本文を読む）

## [v2.2.0] - 2026-09-25

プレリリース。スタックチャン本体の画面だけで Wi-Fi とプリンター（MQTT）を設定できるようになりました。

### Added

- 本体のタッチキーボード（英字・大文字/大文字固定・記号ページ・IP 用の数字キー、後退キーの長押し連続削除、
  パスワードの伏せ字と表示切り替え）
- Wi-Fi 設定画面: 周辺の Wi-Fi を電波の強い順に一覧（鍵・電波マーク、ページ送り、再スキャン、SSID の手入力）。
  パスワードを入れると実際につないで確かめてから保存・再起動。失敗したら入れなおす / このまま保存 / やめる
- プリンター（MQTT）設定画面: 監視 ON/OFF・IP アドレス・シリアル番号・アクセスコード・印刷で自動 MQTT モード
- メニューに「Wi-Fi」「MQTT 設定」タイル（3列×3段）
- はじめての起動（Wi-Fi 未設定）は本体に Wi-Fi 設定画面を出す。セットアップ AP 中は顔をタップで開ける

### Changed

- メニューの「設定」を「接続情報」に改名
- プリンター未設定のときにメニューの「MQTT」「プリンター」を押すと、プリンター設定画面を開く
- セットアップ AP 中の吹き出しは「TAP: WIFI SETUP」と「SETUP: 192.168.4.1」を交互に表示

## [v2.1.0] - 2026-09-25

プレリリース。プリンターが動き出したら自動で MQTT モード（プリンター実況）に切り替わります。

### Added

- MQTT モード（プリンター実況）。顔に HUD を出し、頭タップで状況報告、実況をすべて喋る
- 印刷の準備・印刷・一時停止に入ったら自動で MQTT モードへ切り替え、終わって 5 分で元のモードへ戻す。
  印刷中に手動で別のモードを選んだら、そのジョブの間は自動で戻さない
- 設定「印刷が始まったら自動で MQTT モードにする」（NVS `auto_mqtt`、既定 ON）
- `POST /api/mode`（`mode=mqtt / llm / level`）と、ダッシュボードのモード表示・切り替えボタン
- `/api/printer` に `mode` と `auto_mode`、`/api/status` に `mode`

### Changed

- 本体メニューを3列に再配置し、1段目をモード（MQTT / LOCAL LLM / LEVEL HOLD）にした
- HUD は MQTT モードの間だけ表示する
- LOCAL LLM / LEVEL HOLD では、工程・温度・定期報告などの細かい実況（Low）は喋らず Web のログに残す
- 頭頂タップは MQTT モードでは状況報告、それ以外は設定した文章
- LEVEL HOLD を始められなかったときは、今のモードのままにする

## [v2.0.0] - 2026-09-25

Printer Commentary Edition。stackchan-mqtt としての最初のリリースです。
Bambu Lab P1S の印刷をスタックチャンが実況し、内蔵ボイスで TTS サーバーなしでも喋ります。

### Added

- Bambu Lab LAN MQTT（TLS :8883）クライアント。専用タスクで接続・自動再接続し、P1 系の差分通知をマージ
- 印刷状況の実況エンジン（開始・工程・温度・進捗・1層目・最終層・残り10分・AMS切替・一時停止理由・
  再開・完了・失敗・キャンセル・HMS・接続断・定期報告・頭タップで状況報告）
- 顔に重ねるプリンター HUD（状態・進捗・残り時間・温度・ジョブ名・完成予定）と日本語字幕
- 本体のプリンター詳細画面（顔タップ）、LED 進捗リング
- Web ダッシュボード（`/`）と `/api/printer` 系 API（状況報告・再取得・ライト・実況 ON/OFF・テスト発話）
- プリンター・実況・表示の設定項目（NVS 保存）、NTP による完成予定時刻
- Gateway `/synthesis` の `__SAY__<文章>` 読み上げ
- 夜間モード（既定 23時〜7時は重要な実況だけ声に出す）とスピーカー音量の設定
- 本体メニューの「設定」画面を刷新（ダッシュボード URL・ホットスポット・プリンター接続状態）
- 内蔵ボイス: `tools/make_voice_pack.py` で VOICEVOX のずんだもんの声をボイスパック化し、
  `pio run -t uploadfs` で本体に焼き込む。TTS サーバーなしで単体でも実況できる
- TTS エンジン「内蔵ボイス」。サーバー指定でもつながらないときは自動で内蔵ボイスに切り替え
- 起動時のあいさつ（内蔵ボイス）

### Changed

- メニューを2×3タイルに再設計（プリンター / 実況の声 を追加）
- 設定・状態ページを日本語化し、共通スタイル（`/app.css`）で再デザイン。設定は `/settings` へ移動
- 起動時のサーボ確認は10秒操作がなければ自動で NO（無人再起動で止まらない）
- 頭頂シングルタップはプリンター監視中は状況報告
- メニュー・詳細画面用の 16bit canvas を PSRAM に配置（TLS 用の内部 RAM を確保）
- パーティション構成を変更（アプリ 3MB + LittleFS 12.8MB、NVS の位置は据え置き）
- TTS サーバーへの接続タイムアウトを 30 秒から 4 秒に短縮

## [v1.1.5] - 2026-06-28

Gaming RGB Edition。設定画面からのローカルLLMチャットUIと、顔・本体LEDの
虹色循環演出を追加したリリースです。

### Added

- 設定ページに **Local LLM Chat** カードを追加。ブラウザの `fetch` から直接
  Android Gateway の `http://<TTS Host>:<TTS Port>/ask` へ質問をPOSTし、
  LLM回答（`question` / `answer` / `stackchan`）を画面表示しつつStackChanに発話させる
  （ESP32側に中継APIは作らず、JSON処理はブラウザ側で実施）
- **Gaming RGB**: 顔（画面）と本体LEDを虹色にゆっくり循環させる演出
  （約6秒で1周、顔とLEDの色相を同期、colorDepth=1の2トーン描画では背景と前景を
  補色関係で循環）
- Gaming RGB の有効/無効を設定ページのチェックボックスで切り替え（NVS `gaming_rgb` に保存）
- `/status` ページの App カードに Gaming RGB の On/Off 表示を追加

### Changed

- ステータスLED表示を `showStatusLed()` 経由へ変更し、Gaming RGB有効時はイベント色を
  一定時間保持してから虹色サイクルへ復帰（状態表示の意味を維持）

## [β0.2.7 - v1.1.0] - 中間リリース

タグ `β0.2.7` 〜 `v1.1.0` でリリース済み。主な追加は以下（詳細は各リリースノート参照）。

### Added

- 起動ごとに全キャリブレーション実行を確認する `YES / NO` タッチUI
- `NO` 選択時はサーボを動かさず、電源・トルクOFFのまま起動を続行
- `YES` 選択時はYaw/Pitch原点設定、全可動域、IMU水平の全キャリブレーションを開始
- 全キャリブレーション完了後またはエラー停止後にサーボを安全停止
- 全キャリブレーション処理によるYaw/Pitch原点設定と全可動域の段階的な低速検証
- 全キャリブレーション処理の `START` 後は各端点確認を挟まず、HOME復帰まで自動進行
- Pitch上げ前に必ず中央へ戻し、Pitch `90.0°` は10秒停止でP90完了扱い
- サーボ移動中の画面タッチ緊急停止、到達誤差・タイムアウト検出
- サーボ起動確認画面とキャリブレーション画面を `M5Canvas` 転送へ切り替えてちらつきを低減
- BMI270ジャイロ校正と、300サンプル平均による水平Roll/Pitchゼロ点保存
- 設定画面の音声合成設定をTTS表記へ一般化し、`voicevox_compatible` / `simple_wav` を選択可能に変更
- Speaker / Style IDを文字列保存へ変更し、AivisSpeechなどの大きなIDに対応
- 画面下端から上スワイプで開くモード選択メニューを追加
- デフォルト `LOCAL LLM` モードと、IMU/PID制御で水平維持する `LEVEL HOLD` モードを追加

## [β0.2.5] - 2026-06-23

M5Stack-Avatarによる顔機能の全面実装と、起動時に顔が表示されない問題を修正した
βリリースです。

### Changed

- 独自の顔描画をM5Stack-Avatar v0.10.0へ置き換え
- VOICEVOXリップシンクを `Avatar::setMouthOpenRatio()` へ統合
- Bボタンで全6表情を順番に切り替え
- Cボタンで全7顔テンプレートを順番に切り替え

### Added

- `Happy`、`Angry`、`Sad`、`Doubt`、`Sleepy`、`Neutral`
- Default、Simple、Omega、Girly、Girly2、PinkDemon、Doggy
- 自動瞬き、両目開き、左右ウィンク、両目閉じ
- Default、Skin、Cyber、Monochrome、Demon配色
- Normal、Zoom In、Zoom Out、Tilt Left、Tilt Right変形
- ランダム視線、呼吸、吹き出し
- 頭頂タップ、スワイプ、長押しによる全顔操作
- 全パターンを自動表示するショーケース
- 無操作時に標準顔・通常表情へ戻る待機状態
- 3〜10秒のランダム間隔と100〜180 msの閉眼時間による瞬き
- Wi-Fi接続待ち・設定モード中も標準顔を先に表示する起動順序
- 画面輝度の明示設定と起動段階のシリアル診断ログ
- `THIRD_PARTY_NOTICES.md`
- StackChan BSPより先にCoreS3液晶へ簡易顔を描く安全起動。周辺機器初期化で停止しても液晶状態を確認可能
- Avatar描画タスク開始前の表情設定によってsetupタスクが停止し、顔が表示されなかった初期化順不具合を修正

## [β0.1.0] - 2026-06-22

初回βリリース。

### Added

- CoreS3液晶へStackChanの顔を描画
- 通常、喜び、怒り、悲しみ、眠気の表情
- LGFX Spriteによるちらつきの少ない画面更新
- VOICEVOX Engineの `/audio_query` と `/synthesis` API連携
- VOICEVOX style ID `3` を初期値とした、ずんだもん音声
- 16-bit PCM WAVのストリーミング解析・再生
- PCM平均振幅に基づくリップシンク
- Aボタンと頭頂タッチによる発話
- RGB LEDによる準備・発話・エラー状態表示
- NVSへ保存するブラウザ設定画面
- 周辺Wi-Fiのスキャンと選択
- SSID、RSSI、電波強度、暗号化有無の表示
- 非公開SSIDの手入力
- VOICEVOX host、port、style ID、発話文章の設定
- StackChan-BSP 1.1.0
- M5CoreS3 1.0.1
- カメラ、近接・環境光、マイク、IMU、RTC、microSD用ドライバ
- サーボ、RGB、頭頂タッチ、INA226、NFC、IR用ドライバ
- Wi-Fi、BLE、USB、GPIO、UART、I2C用ライブラリ

### Safety

- 起動直後にサーボのトルクと電源を無効化
- 設定ページで保存済みWi-Fiパスワードを非表示
- パスワード欄が空の場合は保存済みパスワードを維持

### Known limitations

- 音声合成にはLAN上のVOICEVOX Engineが必要
- 音声認識、会話AI、OTAは未実装
- サーボ、カメラ、NFC、IR等はライブラリ準備段階
- β版のためAPIや設定項目は今後変更される可能性あり

[Unreleased]: https://github.com/Xenoah/stackchan-codex/compare/v1.1.5...HEAD
[v1.1.5]: https://github.com/Xenoah/stackchan-codex/releases/tag/v1.1.5
[β0.2.5]: https://github.com/Xenoah/stackchan-codex/releases/tag/v0.2.5-beta.1
[β0.1.0]: https://github.com/Xenoah/stackchan-codex/releases/tag/v0.1.0-beta.1
